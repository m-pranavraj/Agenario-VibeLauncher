{
  "version": 2,
  "buildCommand": "pnpm install --frozen-lockfile && pnpm run build:vercel",
  "outputDirectory": "artifacts/agenario/dist/public",
  "functions": {
    "api/handler.mjs": {
      "maxDuration": 60,
      "memory": 1024,
      "runtime": "nodejs20.x"
    }
  },
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/handler" },
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "headers": [
    {
      "source": "/api/(.*)",
      "headers": [
        { "key": "Cache-Control", "value": "no-store" }
      ]
    }
  ]
}
